# Base da API. No modo demonstração o valor é irrelevante — todas as chamadas
# são resolvidas em memória pelo adapter em src/demo/adapter.ts.
VITE_API_BASE_URL=/api

# Versão exibida no rodapé
VITE_APP_VERSION=demo

# Opcional: link do repositório exibido na faixa de demonstração
VITE_REPO_URL=
